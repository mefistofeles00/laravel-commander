/**
 * Reads and writes .env files.
 *
 * TODO Phase 2:
 * - Parse/write preserving comments and key order
 * - Diff against .env.example (missing/extra keys)
 * - Smart input hints for known keys (APP_DEBUG -> toggle,
 *   DB_CONNECTION -> dropdown)
 */
export class EnvFileService {}
