/**
 * Manages the list of registered Laravel projects.
 *
 * TODO Phase 1:
 * - Add project via folder picker dialog
 * - Validate a folder is a Laravel project (`artisan` file exists and
 *   composer.json requires `laravel/framework`)
 * - Read project meta: Laravel/PHP version, package list from composer.lock
 * - Persist the project list with electron-store
 */
export class ProjectManager {}
