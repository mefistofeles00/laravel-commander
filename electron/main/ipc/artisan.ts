/**
 * Artisan IPC handlers.
 * TODO Phase 2: artisan:list (command catalog via `php artisan list
 * --format=json`), artisan:run (streamed via CommandRunner), artisan:cancel.
 */
export function registerArtisanIpc(): void {}
