/**
 * .env file IPC handlers.
 * TODO Phase 2: env:read, env:write (order/comment preserving),
 * env:diffExample — backed by EnvFileService.
 */
export function registerEnvIpc(): void {
  // env:read / env:write handlers land in Phase 2.
}
